/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.formance.formance_sdk.models.payments;

import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;

/**
 * V3OrderStatusEnum
 * 
 * <p>Lifecycle of an order on the exchange.
 * `PENDING` — accepted by the exchange, not yet working.
 * `OPEN` — live on the book, no fills yet.
 * `PARTIALLY_FILLED` — live on the book, some base quantity filled.
 * `FILLED` — fully filled, terminal.
 * `CANCELLED` — cancelled by the user or system, terminal.
 * `FAILED` — rejected by the exchange, terminal. See `error` for details.
 * `EXPIRED` — `timeInForce` elapsed before full fill, terminal.
 */
public enum V3OrderStatusEnum {
    UNKNOWN("UNKNOWN"),
    PENDING("PENDING"),
    OPEN("OPEN"),
    PARTIALLY_FILLED("PARTIALLY_FILLED"),
    FILLED("FILLED"),
    CANCELLED("CANCELLED"),
    FAILED("FAILED"),
    EXPIRED("EXPIRED");

    @JsonValue
    private final String value;

    V3OrderStatusEnum(String value) {
        this.value = value;
    }
    
    public String value() {
        return value;
    }
    
    public static Optional<V3OrderStatusEnum> fromValue(String value) {
        for (V3OrderStatusEnum o: V3OrderStatusEnum.values()) {
            if (Objects.deepEquals(o.value, value)) {
                return Optional.of(o);
            }
        }
        return Optional.empty();
    }
}

