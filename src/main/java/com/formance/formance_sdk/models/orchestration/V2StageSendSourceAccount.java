/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.formance.formance_sdk.models.orchestration;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.core.type.TypeReference;
import com.formance.formance_sdk.utils.LazySingletonValue;
import com.formance.formance_sdk.utils.Utils;
import java.lang.Boolean;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;


public class V2StageSendSourceAccount {
    /**
     * Enables unbounded overdraft on the throughAccount when set to true.
     * This is useful when the throughAccount represents a liability or bridge account
     * that needs to go negative (e.g., "liabilities:payouts-pending").
     * Only applies when throughAccount is not "world" (which already has unbounded overdraft).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("allowOverdraft")
    private Optional<Boolean> allowOverdraft;


    @JsonProperty("id")
    private String id;


    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("ledger")
    private Optional<String> ledger;

    /**
     * Account used when this ledger account interacts with external systems (payments, cross-ledger).
     * - As SOURCE going to payment: funds are sent to this account (e.g., "liabilities:payouts-pending")
     * - As DESTINATION from payment: funds come from this account (e.g., "assets:stripe:incoming")
     * - For cross-ledger transfers: replaces "world" on both sides
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("throughAccount")
    private Optional<String> throughAccount;

    @JsonCreator
    public V2StageSendSourceAccount(
            @JsonProperty("allowOverdraft") Optional<Boolean> allowOverdraft,
            @JsonProperty("id") String id,
            @JsonProperty("ledger") Optional<String> ledger,
            @JsonProperty("throughAccount") Optional<String> throughAccount) {
        Utils.checkNotNull(allowOverdraft, "allowOverdraft");
        Utils.checkNotNull(id, "id");
        Utils.checkNotNull(ledger, "ledger");
        Utils.checkNotNull(throughAccount, "throughAccount");
        this.allowOverdraft = allowOverdraft;
        this.id = id;
        this.ledger = ledger;
        this.throughAccount = throughAccount;
    }
    
    public V2StageSendSourceAccount(
            String id) {
        this(Optional.empty(), id, Optional.empty(),
            Optional.empty());
    }

    /**
     * Enables unbounded overdraft on the throughAccount when set to true.
     * This is useful when the throughAccount represents a liability or bridge account
     * that needs to go negative (e.g., "liabilities:payouts-pending").
     * Only applies when throughAccount is not "world" (which already has unbounded overdraft).
     */
    @JsonIgnore
    public Optional<Boolean> allowOverdraft() {
        return allowOverdraft;
    }

    @JsonIgnore
    public String id() {
        return id;
    }

    @JsonIgnore
    public Optional<String> ledger() {
        return ledger;
    }

    /**
     * Account used when this ledger account interacts with external systems (payments, cross-ledger).
     * - As SOURCE going to payment: funds are sent to this account (e.g., "liabilities:payouts-pending")
     * - As DESTINATION from payment: funds come from this account (e.g., "assets:stripe:incoming")
     * - For cross-ledger transfers: replaces "world" on both sides
     */
    @JsonIgnore
    public Optional<String> throughAccount() {
        return throughAccount;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Enables unbounded overdraft on the throughAccount when set to true.
     * This is useful when the throughAccount represents a liability or bridge account
     * that needs to go negative (e.g., "liabilities:payouts-pending").
     * Only applies when throughAccount is not "world" (which already has unbounded overdraft).
     */
    public V2StageSendSourceAccount withAllowOverdraft(boolean allowOverdraft) {
        Utils.checkNotNull(allowOverdraft, "allowOverdraft");
        this.allowOverdraft = Optional.ofNullable(allowOverdraft);
        return this;
    }


    /**
     * Enables unbounded overdraft on the throughAccount when set to true.
     * This is useful when the throughAccount represents a liability or bridge account
     * that needs to go negative (e.g., "liabilities:payouts-pending").
     * Only applies when throughAccount is not "world" (which already has unbounded overdraft).
     */
    public V2StageSendSourceAccount withAllowOverdraft(Optional<Boolean> allowOverdraft) {
        Utils.checkNotNull(allowOverdraft, "allowOverdraft");
        this.allowOverdraft = allowOverdraft;
        return this;
    }

    public V2StageSendSourceAccount withId(String id) {
        Utils.checkNotNull(id, "id");
        this.id = id;
        return this;
    }

    public V2StageSendSourceAccount withLedger(String ledger) {
        Utils.checkNotNull(ledger, "ledger");
        this.ledger = Optional.ofNullable(ledger);
        return this;
    }


    public V2StageSendSourceAccount withLedger(Optional<String> ledger) {
        Utils.checkNotNull(ledger, "ledger");
        this.ledger = ledger;
        return this;
    }

    /**
     * Account used when this ledger account interacts with external systems (payments, cross-ledger).
     * - As SOURCE going to payment: funds are sent to this account (e.g., "liabilities:payouts-pending")
     * - As DESTINATION from payment: funds come from this account (e.g., "assets:stripe:incoming")
     * - For cross-ledger transfers: replaces "world" on both sides
     */
    public V2StageSendSourceAccount withThroughAccount(String throughAccount) {
        Utils.checkNotNull(throughAccount, "throughAccount");
        this.throughAccount = Optional.ofNullable(throughAccount);
        return this;
    }


    /**
     * Account used when this ledger account interacts with external systems (payments, cross-ledger).
     * - As SOURCE going to payment: funds are sent to this account (e.g., "liabilities:payouts-pending")
     * - As DESTINATION from payment: funds come from this account (e.g., "assets:stripe:incoming")
     * - For cross-ledger transfers: replaces "world" on both sides
     */
    public V2StageSendSourceAccount withThroughAccount(Optional<String> throughAccount) {
        Utils.checkNotNull(throughAccount, "throughAccount");
        this.throughAccount = throughAccount;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        V2StageSendSourceAccount other = (V2StageSendSourceAccount) o;
        return 
            Utils.enhancedDeepEquals(this.allowOverdraft, other.allowOverdraft) &&
            Utils.enhancedDeepEquals(this.id, other.id) &&
            Utils.enhancedDeepEquals(this.ledger, other.ledger) &&
            Utils.enhancedDeepEquals(this.throughAccount, other.throughAccount);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            allowOverdraft, id, ledger,
            throughAccount);
    }
    
    @Override
    public String toString() {
        return Utils.toString(V2StageSendSourceAccount.class,
                "allowOverdraft", allowOverdraft,
                "id", id,
                "ledger", ledger,
                "throughAccount", throughAccount);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private Optional<Boolean> allowOverdraft;

        private String id;

        private Optional<String> ledger = Optional.empty();

        private Optional<String> throughAccount;

        private Builder() {
          // force use of static builder() method
        }


        /**
         * Enables unbounded overdraft on the throughAccount when set to true.
         * This is useful when the throughAccount represents a liability or bridge account
         * that needs to go negative (e.g., "liabilities:payouts-pending").
         * Only applies when throughAccount is not "world" (which already has unbounded overdraft).
         */
        public Builder allowOverdraft(boolean allowOverdraft) {
            Utils.checkNotNull(allowOverdraft, "allowOverdraft");
            this.allowOverdraft = Optional.ofNullable(allowOverdraft);
            return this;
        }

        /**
         * Enables unbounded overdraft on the throughAccount when set to true.
         * This is useful when the throughAccount represents a liability or bridge account
         * that needs to go negative (e.g., "liabilities:payouts-pending").
         * Only applies when throughAccount is not "world" (which already has unbounded overdraft).
         */
        public Builder allowOverdraft(Optional<Boolean> allowOverdraft) {
            Utils.checkNotNull(allowOverdraft, "allowOverdraft");
            this.allowOverdraft = allowOverdraft;
            return this;
        }


        public Builder id(String id) {
            Utils.checkNotNull(id, "id");
            this.id = id;
            return this;
        }


        public Builder ledger(String ledger) {
            Utils.checkNotNull(ledger, "ledger");
            this.ledger = Optional.ofNullable(ledger);
            return this;
        }

        public Builder ledger(Optional<String> ledger) {
            Utils.checkNotNull(ledger, "ledger");
            this.ledger = ledger;
            return this;
        }


        /**
         * Account used when this ledger account interacts with external systems (payments, cross-ledger).
         * - As SOURCE going to payment: funds are sent to this account (e.g., "liabilities:payouts-pending")
         * - As DESTINATION from payment: funds come from this account (e.g., "assets:stripe:incoming")
         * - For cross-ledger transfers: replaces "world" on both sides
         */
        public Builder throughAccount(String throughAccount) {
            Utils.checkNotNull(throughAccount, "throughAccount");
            this.throughAccount = Optional.ofNullable(throughAccount);
            return this;
        }

        /**
         * Account used when this ledger account interacts with external systems (payments, cross-ledger).
         * - As SOURCE going to payment: funds are sent to this account (e.g., "liabilities:payouts-pending")
         * - As DESTINATION from payment: funds come from this account (e.g., "assets:stripe:incoming")
         * - For cross-ledger transfers: replaces "world" on both sides
         */
        public Builder throughAccount(Optional<String> throughAccount) {
            Utils.checkNotNull(throughAccount, "throughAccount");
            this.throughAccount = throughAccount;
            return this;
        }

        public V2StageSendSourceAccount build() {
            if (allowOverdraft == null) {
                allowOverdraft = _SINGLETON_VALUE_AllowOverdraft.value();
            }
            if (throughAccount == null) {
                throughAccount = _SINGLETON_VALUE_ThroughAccount.value();
            }

            return new V2StageSendSourceAccount(
                allowOverdraft, id, ledger,
                throughAccount);
        }


        private static final LazySingletonValue<Optional<Boolean>> _SINGLETON_VALUE_AllowOverdraft =
                new LazySingletonValue<>(
                        "allowOverdraft",
                        "false",
                        new TypeReference<Optional<Boolean>>() {});

        private static final LazySingletonValue<Optional<String>> _SINGLETON_VALUE_ThroughAccount =
                new LazySingletonValue<>(
                        "throughAccount",
                        "\"world\"",
                        new TypeReference<Optional<String>>() {});
    }
}
