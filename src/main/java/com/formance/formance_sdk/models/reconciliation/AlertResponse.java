/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.formance.formance_sdk.models.reconciliation;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.formance.formance_sdk.utils.Utils;
import java.lang.Override;
import java.lang.String;

/**
 * AlertResponse
 * 
 * <p>OK
 */
public class AlertResponse {
    /**
     * An Alert is the stable, dedup'd record of a failing fingerprint within a
     * reconciliation period. There is exactly one Alert per
     * (ruleID, fingerprint, periodID) — within a period, re-opens after resolve
     * flip the status back to OPEN in place; the same fingerprint failing in a
     * new period is a separate Alert. For a `continuous`-cadence rule there is
     * one unbounded period, so it behaves as one Alert per (ruleID, fingerprint).
     * The full history of transitions lives in alert_event
     * (see /alerts/{alertID}/events).
     */
    @JsonProperty("data")
    private Alert data;

    @JsonCreator
    public AlertResponse(
            @JsonProperty("data") Alert data) {
        Utils.checkNotNull(data, "data");
        this.data = data;
    }

    /**
     * An Alert is the stable, dedup'd record of a failing fingerprint within a
     * reconciliation period. There is exactly one Alert per
     * (ruleID, fingerprint, periodID) — within a period, re-opens after resolve
     * flip the status back to OPEN in place; the same fingerprint failing in a
     * new period is a separate Alert. For a `continuous`-cadence rule there is
     * one unbounded period, so it behaves as one Alert per (ruleID, fingerprint).
     * The full history of transitions lives in alert_event
     * (see /alerts/{alertID}/events).
     */
    @JsonIgnore
    public Alert data() {
        return data;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * An Alert is the stable, dedup'd record of a failing fingerprint within a
     * reconciliation period. There is exactly one Alert per
     * (ruleID, fingerprint, periodID) — within a period, re-opens after resolve
     * flip the status back to OPEN in place; the same fingerprint failing in a
     * new period is a separate Alert. For a `continuous`-cadence rule there is
     * one unbounded period, so it behaves as one Alert per (ruleID, fingerprint).
     * The full history of transitions lives in alert_event
     * (see /alerts/{alertID}/events).
     */
    public AlertResponse withData(Alert data) {
        Utils.checkNotNull(data, "data");
        this.data = data;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AlertResponse other = (AlertResponse) o;
        return 
            Utils.enhancedDeepEquals(this.data, other.data);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            data);
    }
    
    @Override
    public String toString() {
        return Utils.toString(AlertResponse.class,
                "data", data);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private Alert data;

        private Builder() {
          // force use of static builder() method
        }


        /**
         * An Alert is the stable, dedup'd record of a failing fingerprint within a
         * reconciliation period. There is exactly one Alert per
         * (ruleID, fingerprint, periodID) — within a period, re-opens after resolve
         * flip the status back to OPEN in place; the same fingerprint failing in a
         * new period is a separate Alert. For a `continuous`-cadence rule there is
         * one unbounded period, so it behaves as one Alert per (ruleID, fingerprint).
         * The full history of transitions lives in alert_event
         * (see /alerts/{alertID}/events).
         */
        public Builder data(Alert data) {
            Utils.checkNotNull(data, "data");
            this.data = data;
            return this;
        }

        public AlertResponse build() {

            return new AlertResponse(
                data);
        }

    }
}
