/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.formance.formance_sdk.models.reconciliation;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.formance.formance_sdk.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.time.OffsetDateTime;
import java.util.Map;
import java.util.Optional;


public class EvaluateRuleRequest {
    /**
     * Default PIT for every source and canonical PIT for alert-period scoping. Defaults to now, but is
     * required when `sourcePITs` is provided. Must be in the past.
     * 
     * <p>A supplied `at` is read point-in-time on the payments pool; the default (now) reads the pool's
     * latest snapshot.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("at")
    private Optional<OffsetDateTime> at;

    /**
     * Go duration string subtracted from the default `at` PIT. Defaults to 30s. It is not subtracted from
     * already-effective `sourcePITs` replay values.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("safetyMargin")
    private Optional<String> safetyMargin;

    /**
     * Optional per-source PIT overrides, keyed by the template's stable source key ("&lt;label&gt;#<idx>",
     * as echoed back in Evaluation.pitPerSource — e.g. "ledger:main#0", "pool:acct#0"). A source with an
     * override reads at exactly that instant instead of `at` (the safety margin is not applied again);
     * this expresses the two-independent-timestamps contract (ledger at one instant, payments pool at
     * another, to absorb inter-system settlement lag).
     * 
     * <p>`at` is required whenever this object is supplied so the evaluation has an unambiguous PIT for
     * daily/monthly alert-period scoping. Every value must be in the past. A key that names no source of
     * the rule's template is rejected with 400 (it is not silently ignored).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("sourcePITs")
    private Optional<? extends Map<String, OffsetDateTime>> sourcePITs;

    @JsonCreator
    public EvaluateRuleRequest(
            @JsonProperty("at") Optional<OffsetDateTime> at,
            @JsonProperty("safetyMargin") Optional<String> safetyMargin,
            @JsonProperty("sourcePITs") Optional<? extends Map<String, OffsetDateTime>> sourcePITs) {
        Utils.checkNotNull(at, "at");
        Utils.checkNotNull(safetyMargin, "safetyMargin");
        Utils.checkNotNull(sourcePITs, "sourcePITs");
        this.at = at;
        this.safetyMargin = safetyMargin;
        this.sourcePITs = sourcePITs;
    }
    
    public EvaluateRuleRequest() {
        this(Optional.empty(), Optional.empty(), Optional.empty());
    }

    /**
     * Default PIT for every source and canonical PIT for alert-period scoping. Defaults to now, but is
     * required when `sourcePITs` is provided. Must be in the past.
     * 
     * <p>A supplied `at` is read point-in-time on the payments pool; the default (now) reads the pool's
     * latest snapshot.
     */
    @JsonIgnore
    public Optional<OffsetDateTime> at() {
        return at;
    }

    /**
     * Go duration string subtracted from the default `at` PIT. Defaults to 30s. It is not subtracted from
     * already-effective `sourcePITs` replay values.
     */
    @JsonIgnore
    public Optional<String> safetyMargin() {
        return safetyMargin;
    }

    /**
     * Optional per-source PIT overrides, keyed by the template's stable source key ("&lt;label&gt;#<idx>",
     * as echoed back in Evaluation.pitPerSource — e.g. "ledger:main#0", "pool:acct#0"). A source with an
     * override reads at exactly that instant instead of `at` (the safety margin is not applied again);
     * this expresses the two-independent-timestamps contract (ledger at one instant, payments pool at
     * another, to absorb inter-system settlement lag).
     * 
     * <p>`at` is required whenever this object is supplied so the evaluation has an unambiguous PIT for
     * daily/monthly alert-period scoping. Every value must be in the past. A key that names no source of
     * the rule's template is rejected with 400 (it is not silently ignored).
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<Map<String, OffsetDateTime>> sourcePITs() {
        return (Optional<Map<String, OffsetDateTime>>) sourcePITs;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * Default PIT for every source and canonical PIT for alert-period scoping. Defaults to now, but is
     * required when `sourcePITs` is provided. Must be in the past.
     * 
     * <p>A supplied `at` is read point-in-time on the payments pool; the default (now) reads the pool's
     * latest snapshot.
     */
    public EvaluateRuleRequest withAt(OffsetDateTime at) {
        Utils.checkNotNull(at, "at");
        this.at = Optional.ofNullable(at);
        return this;
    }


    /**
     * Default PIT for every source and canonical PIT for alert-period scoping. Defaults to now, but is
     * required when `sourcePITs` is provided. Must be in the past.
     * 
     * <p>A supplied `at` is read point-in-time on the payments pool; the default (now) reads the pool's
     * latest snapshot.
     */
    public EvaluateRuleRequest withAt(Optional<OffsetDateTime> at) {
        Utils.checkNotNull(at, "at");
        this.at = at;
        return this;
    }

    /**
     * Go duration string subtracted from the default `at` PIT. Defaults to 30s. It is not subtracted from
     * already-effective `sourcePITs` replay values.
     */
    public EvaluateRuleRequest withSafetyMargin(String safetyMargin) {
        Utils.checkNotNull(safetyMargin, "safetyMargin");
        this.safetyMargin = Optional.ofNullable(safetyMargin);
        return this;
    }


    /**
     * Go duration string subtracted from the default `at` PIT. Defaults to 30s. It is not subtracted from
     * already-effective `sourcePITs` replay values.
     */
    public EvaluateRuleRequest withSafetyMargin(Optional<String> safetyMargin) {
        Utils.checkNotNull(safetyMargin, "safetyMargin");
        this.safetyMargin = safetyMargin;
        return this;
    }

    /**
     * Optional per-source PIT overrides, keyed by the template's stable source key ("&lt;label&gt;#<idx>",
     * as echoed back in Evaluation.pitPerSource — e.g. "ledger:main#0", "pool:acct#0"). A source with an
     * override reads at exactly that instant instead of `at` (the safety margin is not applied again);
     * this expresses the two-independent-timestamps contract (ledger at one instant, payments pool at
     * another, to absorb inter-system settlement lag).
     * 
     * <p>`at` is required whenever this object is supplied so the evaluation has an unambiguous PIT for
     * daily/monthly alert-period scoping. Every value must be in the past. A key that names no source of
     * the rule's template is rejected with 400 (it is not silently ignored).
     */
    public EvaluateRuleRequest withSourcePITs(Map<String, OffsetDateTime> sourcePITs) {
        Utils.checkNotNull(sourcePITs, "sourcePITs");
        this.sourcePITs = Optional.ofNullable(sourcePITs);
        return this;
    }


    /**
     * Optional per-source PIT overrides, keyed by the template's stable source key ("&lt;label&gt;#<idx>",
     * as echoed back in Evaluation.pitPerSource — e.g. "ledger:main#0", "pool:acct#0"). A source with an
     * override reads at exactly that instant instead of `at` (the safety margin is not applied again);
     * this expresses the two-independent-timestamps contract (ledger at one instant, payments pool at
     * another, to absorb inter-system settlement lag).
     * 
     * <p>`at` is required whenever this object is supplied so the evaluation has an unambiguous PIT for
     * daily/monthly alert-period scoping. Every value must be in the past. A key that names no source of
     * the rule's template is rejected with 400 (it is not silently ignored).
     */
    public EvaluateRuleRequest withSourcePITs(Optional<? extends Map<String, OffsetDateTime>> sourcePITs) {
        Utils.checkNotNull(sourcePITs, "sourcePITs");
        this.sourcePITs = sourcePITs;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        EvaluateRuleRequest other = (EvaluateRuleRequest) o;
        return 
            Utils.enhancedDeepEquals(this.at, other.at) &&
            Utils.enhancedDeepEquals(this.safetyMargin, other.safetyMargin) &&
            Utils.enhancedDeepEquals(this.sourcePITs, other.sourcePITs);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            at, safetyMargin, sourcePITs);
    }
    
    @Override
    public String toString() {
        return Utils.toString(EvaluateRuleRequest.class,
                "at", at,
                "safetyMargin", safetyMargin,
                "sourcePITs", sourcePITs);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private Optional<OffsetDateTime> at = Optional.empty();

        private Optional<String> safetyMargin = Optional.empty();

        private Optional<? extends Map<String, OffsetDateTime>> sourcePITs = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * Default PIT for every source and canonical PIT for alert-period scoping. Defaults to now, but is
         * required when `sourcePITs` is provided. Must be in the past.
         * 
         * <p>A supplied `at` is read point-in-time on the payments pool; the default (now) reads the pool's
         * latest snapshot.
         */
        public Builder at(OffsetDateTime at) {
            Utils.checkNotNull(at, "at");
            this.at = Optional.ofNullable(at);
            return this;
        }

        /**
         * Default PIT for every source and canonical PIT for alert-period scoping. Defaults to now, but is
         * required when `sourcePITs` is provided. Must be in the past.
         * 
         * <p>A supplied `at` is read point-in-time on the payments pool; the default (now) reads the pool's
         * latest snapshot.
         */
        public Builder at(Optional<OffsetDateTime> at) {
            Utils.checkNotNull(at, "at");
            this.at = at;
            return this;
        }


        /**
         * Go duration string subtracted from the default `at` PIT. Defaults to 30s. It is not subtracted from
         * already-effective `sourcePITs` replay values.
         */
        public Builder safetyMargin(String safetyMargin) {
            Utils.checkNotNull(safetyMargin, "safetyMargin");
            this.safetyMargin = Optional.ofNullable(safetyMargin);
            return this;
        }

        /**
         * Go duration string subtracted from the default `at` PIT. Defaults to 30s. It is not subtracted from
         * already-effective `sourcePITs` replay values.
         */
        public Builder safetyMargin(Optional<String> safetyMargin) {
            Utils.checkNotNull(safetyMargin, "safetyMargin");
            this.safetyMargin = safetyMargin;
            return this;
        }


        /**
         * Optional per-source PIT overrides, keyed by the template's stable source key ("&lt;label&gt;#<idx>",
         * as echoed back in Evaluation.pitPerSource — e.g. "ledger:main#0", "pool:acct#0"). A source with an
         * override reads at exactly that instant instead of `at` (the safety margin is not applied again);
         * this expresses the two-independent-timestamps contract (ledger at one instant, payments pool at
         * another, to absorb inter-system settlement lag).
         * 
         * <p>`at` is required whenever this object is supplied so the evaluation has an unambiguous PIT for
         * daily/monthly alert-period scoping. Every value must be in the past. A key that names no source of
         * the rule's template is rejected with 400 (it is not silently ignored).
         */
        public Builder sourcePITs(Map<String, OffsetDateTime> sourcePITs) {
            Utils.checkNotNull(sourcePITs, "sourcePITs");
            this.sourcePITs = Optional.ofNullable(sourcePITs);
            return this;
        }

        /**
         * Optional per-source PIT overrides, keyed by the template's stable source key ("&lt;label&gt;#<idx>",
         * as echoed back in Evaluation.pitPerSource — e.g. "ledger:main#0", "pool:acct#0"). A source with an
         * override reads at exactly that instant instead of `at` (the safety margin is not applied again);
         * this expresses the two-independent-timestamps contract (ledger at one instant, payments pool at
         * another, to absorb inter-system settlement lag).
         * 
         * <p>`at` is required whenever this object is supplied so the evaluation has an unambiguous PIT for
         * daily/monthly alert-period scoping. Every value must be in the past. A key that names no source of
         * the rule's template is rejected with 400 (it is not silently ignored).
         */
        public Builder sourcePITs(Optional<? extends Map<String, OffsetDateTime>> sourcePITs) {
            Utils.checkNotNull(sourcePITs, "sourcePITs");
            this.sourcePITs = sourcePITs;
            return this;
        }

        public EvaluateRuleRequest build() {

            return new EvaluateRuleRequest(
                at, safetyMargin, sourcePITs);
        }

    }
}
