/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.formance.formance_sdk.models.reconciliation;

import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;

/**
 * Cadence
 * 
 * <p>Reconciliation rhythm. Scopes each failing fingerprint into a period so a
 * March break and an April break are distinct, independently-closable cases.
 * `continuous` (default) is a single unbounded period (live monitoring).
 */
public enum Cadence {
    CONTINUOUS("continuous"),
    DAILY("daily"),
    WEEKLY("weekly"),
    MONTHLY("monthly");

    @JsonValue
    private final String value;

    Cadence(String value) {
        this.value = value;
    }
    
    public String value() {
        return value;
    }
    
    public static Optional<Cadence> fromValue(String value) {
        for (Cadence o: Cadence.values()) {
            if (Objects.deepEquals(o.value, value)) {
                return Optional.of(o);
            }
        }
        return Optional.empty();
    }
}

