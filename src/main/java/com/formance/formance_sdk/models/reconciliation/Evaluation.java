/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.formance.formance_sdk.models.reconciliation;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.formance.formance_sdk.utils.Utils;
import java.lang.Long;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.time.OffsetDateTime;
import java.util.Map;
import java.util.Optional;


public class Evaluation {

    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("costUnits")
    private Optional<Long> costUnits;


    @JsonProperty("createdAt")
    private OffsetDateTime createdAt;


    @JsonProperty("endedAt")
    private OffsetDateTime endedAt;


    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("error")
    private Optional<String> error;

    /**
     * For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed
     * balances that make the check hold, with self-describing keys — both sides where two are compared,
     * e.g. {"ledger":"523500","pool":"-523500"} (drift), {"left":…,"right":…} (source_parity),
     * {"positive":…,"negative":…} (ledger_invariant), or {"balance":…} (account_threshold).
     * 
     * <p>These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without
     * recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance,
     * compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked).
     * 
     * <p>An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing
     * the engine failure.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("evidence")
    private Optional<? extends EvidenceUnion> evidence;


    @JsonProperty("id")
    private String id;

    /**
     * Effective historical PITs used by sources. For a Payments `latest` read, the value is the successful
     * observation time because the upstream response has no snapshot timestamp; the evaluation evidence is
     * frozen, but exact historical replay of that latest snapshot is not guaranteed.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("pitPerSource")
    private Optional<? extends Map<String, OffsetDateTime>> pitPerSource;


    @JsonProperty("result")
    private Result result;


    @JsonProperty("ruleID")
    private String ruleID;


    @JsonProperty("startedAt")
    private OffsetDateTime startedAt;

    @JsonCreator
    public Evaluation(
            @JsonProperty("costUnits") Optional<Long> costUnits,
            @JsonProperty("createdAt") OffsetDateTime createdAt,
            @JsonProperty("endedAt") OffsetDateTime endedAt,
            @JsonProperty("error") Optional<String> error,
            @JsonProperty("evidence") Optional<? extends EvidenceUnion> evidence,
            @JsonProperty("id") String id,
            @JsonProperty("pitPerSource") Optional<? extends Map<String, OffsetDateTime>> pitPerSource,
            @JsonProperty("result") Result result,
            @JsonProperty("ruleID") String ruleID,
            @JsonProperty("startedAt") OffsetDateTime startedAt) {
        Utils.checkNotNull(costUnits, "costUnits");
        Utils.checkNotNull(createdAt, "createdAt");
        Utils.checkNotNull(endedAt, "endedAt");
        Utils.checkNotNull(error, "error");
        Utils.checkNotNull(evidence, "evidence");
        Utils.checkNotNull(id, "id");
        Utils.checkNotNull(pitPerSource, "pitPerSource");
        Utils.checkNotNull(result, "result");
        Utils.checkNotNull(ruleID, "ruleID");
        Utils.checkNotNull(startedAt, "startedAt");
        this.costUnits = costUnits;
        this.createdAt = createdAt;
        this.endedAt = endedAt;
        this.error = error;
        this.evidence = evidence;
        this.id = id;
        this.pitPerSource = pitPerSource;
        this.result = result;
        this.ruleID = ruleID;
        this.startedAt = startedAt;
    }
    
    public Evaluation(
            OffsetDateTime createdAt,
            OffsetDateTime endedAt,
            String id,
            Result result,
            String ruleID,
            OffsetDateTime startedAt) {
        this(Optional.empty(), createdAt, endedAt,
            Optional.empty(), Optional.empty(), id,
            Optional.empty(), result, ruleID,
            startedAt);
    }

    @JsonIgnore
    public Optional<Long> costUnits() {
        return costUnits;
    }

    @JsonIgnore
    public OffsetDateTime createdAt() {
        return createdAt;
    }

    @JsonIgnore
    public OffsetDateTime endedAt() {
        return endedAt;
    }

    @JsonIgnore
    public Optional<String> error() {
        return error;
    }

    /**
     * For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed
     * balances that make the check hold, with self-describing keys — both sides where two are compared,
     * e.g. {"ledger":"523500","pool":"-523500"} (drift), {"left":…,"right":…} (source_parity),
     * {"positive":…,"negative":…} (ledger_invariant), or {"balance":…} (account_threshold).
     * 
     * <p>These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without
     * recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance,
     * compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked).
     * 
     * <p>An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing
     * the engine failure.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<EvidenceUnion> evidence() {
        return (Optional<EvidenceUnion>) evidence;
    }

    @JsonIgnore
    public String id() {
        return id;
    }

    /**
     * Effective historical PITs used by sources. For a Payments `latest` read, the value is the successful
     * observation time because the upstream response has no snapshot timestamp; the evaluation evidence is
     * frozen, but exact historical replay of that latest snapshot is not guaranteed.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<Map<String, OffsetDateTime>> pitPerSource() {
        return (Optional<Map<String, OffsetDateTime>>) pitPerSource;
    }

    @JsonIgnore
    public Result result() {
        return result;
    }

    @JsonIgnore
    public String ruleID() {
        return ruleID;
    }

    @JsonIgnore
    public OffsetDateTime startedAt() {
        return startedAt;
    }

    public static Builder builder() {
        return new Builder();
    }


    public Evaluation withCostUnits(long costUnits) {
        Utils.checkNotNull(costUnits, "costUnits");
        this.costUnits = Optional.ofNullable(costUnits);
        return this;
    }


    public Evaluation withCostUnits(Optional<Long> costUnits) {
        Utils.checkNotNull(costUnits, "costUnits");
        this.costUnits = costUnits;
        return this;
    }

    public Evaluation withCreatedAt(OffsetDateTime createdAt) {
        Utils.checkNotNull(createdAt, "createdAt");
        this.createdAt = createdAt;
        return this;
    }

    public Evaluation withEndedAt(OffsetDateTime endedAt) {
        Utils.checkNotNull(endedAt, "endedAt");
        this.endedAt = endedAt;
        return this;
    }

    public Evaluation withError(String error) {
        Utils.checkNotNull(error, "error");
        this.error = Optional.ofNullable(error);
        return this;
    }


    public Evaluation withError(Optional<String> error) {
        Utils.checkNotNull(error, "error");
        this.error = error;
        return this;
    }

    /**
     * For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed
     * balances that make the check hold, with self-describing keys — both sides where two are compared,
     * e.g. {"ledger":"523500","pool":"-523500"} (drift), {"left":…,"right":…} (source_parity),
     * {"positive":…,"negative":…} (ledger_invariant), or {"balance":…} (account_threshold).
     * 
     * <p>These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without
     * recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance,
     * compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked).
     * 
     * <p>An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing
     * the engine failure.
     */
    public Evaluation withEvidence(EvidenceUnion evidence) {
        Utils.checkNotNull(evidence, "evidence");
        this.evidence = Optional.ofNullable(evidence);
        return this;
    }


    /**
     * For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed
     * balances that make the check hold, with self-describing keys — both sides where two are compared,
     * e.g. {"ledger":"523500","pool":"-523500"} (drift), {"left":…,"right":…} (source_parity),
     * {"positive":…,"negative":…} (ledger_invariant), or {"balance":…} (account_threshold).
     * 
     * <p>These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without
     * recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance,
     * compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked).
     * 
     * <p>An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing
     * the engine failure.
     */
    public Evaluation withEvidence(Optional<? extends EvidenceUnion> evidence) {
        Utils.checkNotNull(evidence, "evidence");
        this.evidence = evidence;
        return this;
    }

    public Evaluation withId(String id) {
        Utils.checkNotNull(id, "id");
        this.id = id;
        return this;
    }

    /**
     * Effective historical PITs used by sources. For a Payments `latest` read, the value is the successful
     * observation time because the upstream response has no snapshot timestamp; the evaluation evidence is
     * frozen, but exact historical replay of that latest snapshot is not guaranteed.
     */
    public Evaluation withPitPerSource(Map<String, OffsetDateTime> pitPerSource) {
        Utils.checkNotNull(pitPerSource, "pitPerSource");
        this.pitPerSource = Optional.ofNullable(pitPerSource);
        return this;
    }


    /**
     * Effective historical PITs used by sources. For a Payments `latest` read, the value is the successful
     * observation time because the upstream response has no snapshot timestamp; the evaluation evidence is
     * frozen, but exact historical replay of that latest snapshot is not guaranteed.
     */
    public Evaluation withPitPerSource(Optional<? extends Map<String, OffsetDateTime>> pitPerSource) {
        Utils.checkNotNull(pitPerSource, "pitPerSource");
        this.pitPerSource = pitPerSource;
        return this;
    }

    public Evaluation withResult(Result result) {
        Utils.checkNotNull(result, "result");
        this.result = result;
        return this;
    }

    public Evaluation withRuleID(String ruleID) {
        Utils.checkNotNull(ruleID, "ruleID");
        this.ruleID = ruleID;
        return this;
    }

    public Evaluation withStartedAt(OffsetDateTime startedAt) {
        Utils.checkNotNull(startedAt, "startedAt");
        this.startedAt = startedAt;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        Evaluation other = (Evaluation) o;
        return 
            Utils.enhancedDeepEquals(this.costUnits, other.costUnits) &&
            Utils.enhancedDeepEquals(this.createdAt, other.createdAt) &&
            Utils.enhancedDeepEquals(this.endedAt, other.endedAt) &&
            Utils.enhancedDeepEquals(this.error, other.error) &&
            Utils.enhancedDeepEquals(this.evidence, other.evidence) &&
            Utils.enhancedDeepEquals(this.id, other.id) &&
            Utils.enhancedDeepEquals(this.pitPerSource, other.pitPerSource) &&
            Utils.enhancedDeepEquals(this.result, other.result) &&
            Utils.enhancedDeepEquals(this.ruleID, other.ruleID) &&
            Utils.enhancedDeepEquals(this.startedAt, other.startedAt);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            costUnits, createdAt, endedAt,
            error, evidence, id,
            pitPerSource, result, ruleID,
            startedAt);
    }
    
    @Override
    public String toString() {
        return Utils.toString(Evaluation.class,
                "costUnits", costUnits,
                "createdAt", createdAt,
                "endedAt", endedAt,
                "error", error,
                "evidence", evidence,
                "id", id,
                "pitPerSource", pitPerSource,
                "result", result,
                "ruleID", ruleID,
                "startedAt", startedAt);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private Optional<Long> costUnits = Optional.empty();

        private OffsetDateTime createdAt;

        private OffsetDateTime endedAt;

        private Optional<String> error = Optional.empty();

        private Optional<? extends EvidenceUnion> evidence = Optional.empty();

        private String id;

        private Optional<? extends Map<String, OffsetDateTime>> pitPerSource = Optional.empty();

        private Result result;

        private String ruleID;

        private OffsetDateTime startedAt;

        private Builder() {
          // force use of static builder() method
        }


        public Builder costUnits(long costUnits) {
            Utils.checkNotNull(costUnits, "costUnits");
            this.costUnits = Optional.ofNullable(costUnits);
            return this;
        }

        public Builder costUnits(Optional<Long> costUnits) {
            Utils.checkNotNull(costUnits, "costUnits");
            this.costUnits = costUnits;
            return this;
        }


        public Builder createdAt(OffsetDateTime createdAt) {
            Utils.checkNotNull(createdAt, "createdAt");
            this.createdAt = createdAt;
            return this;
        }


        public Builder endedAt(OffsetDateTime endedAt) {
            Utils.checkNotNull(endedAt, "endedAt");
            this.endedAt = endedAt;
            return this;
        }


        public Builder error(String error) {
            Utils.checkNotNull(error, "error");
            this.error = Optional.ofNullable(error);
            return this;
        }

        public Builder error(Optional<String> error) {
            Utils.checkNotNull(error, "error");
            this.error = error;
            return this;
        }


        /**
         * For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed
         * balances that make the check hold, with self-describing keys — both sides where two are compared,
         * e.g. {"ledger":"523500","pool":"-523500"} (drift), {"left":…,"right":…} (source_parity),
         * {"positive":…,"negative":…} (ledger_invariant), or {"balance":…} (account_threshold).
         * 
         * <p>These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without
         * recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance,
         * compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked).
         * 
         * <p>An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing
         * the engine failure.
         */
        public Builder evidence(EvidenceUnion evidence) {
            Utils.checkNotNull(evidence, "evidence");
            this.evidence = Optional.ofNullable(evidence);
            return this;
        }

        /**
         * For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed
         * balances that make the check hold, with self-describing keys — both sides where two are compared,
         * e.g. {"ledger":"523500","pool":"-523500"} (drift), {"left":…,"right":…} (source_parity),
         * {"positive":…,"negative":…} (ledger_invariant), or {"balance":…} (account_threshold).
         * 
         * <p>These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without
         * recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance,
         * compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked).
         * 
         * <p>An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing
         * the engine failure.
         */
        public Builder evidence(Optional<? extends EvidenceUnion> evidence) {
            Utils.checkNotNull(evidence, "evidence");
            this.evidence = evidence;
            return this;
        }


        public Builder id(String id) {
            Utils.checkNotNull(id, "id");
            this.id = id;
            return this;
        }


        /**
         * Effective historical PITs used by sources. For a Payments `latest` read, the value is the successful
         * observation time because the upstream response has no snapshot timestamp; the evaluation evidence is
         * frozen, but exact historical replay of that latest snapshot is not guaranteed.
         */
        public Builder pitPerSource(Map<String, OffsetDateTime> pitPerSource) {
            Utils.checkNotNull(pitPerSource, "pitPerSource");
            this.pitPerSource = Optional.ofNullable(pitPerSource);
            return this;
        }

        /**
         * Effective historical PITs used by sources. For a Payments `latest` read, the value is the successful
         * observation time because the upstream response has no snapshot timestamp; the evaluation evidence is
         * frozen, but exact historical replay of that latest snapshot is not guaranteed.
         */
        public Builder pitPerSource(Optional<? extends Map<String, OffsetDateTime>> pitPerSource) {
            Utils.checkNotNull(pitPerSource, "pitPerSource");
            this.pitPerSource = pitPerSource;
            return this;
        }


        public Builder result(Result result) {
            Utils.checkNotNull(result, "result");
            this.result = result;
            return this;
        }


        public Builder ruleID(String ruleID) {
            Utils.checkNotNull(ruleID, "ruleID");
            this.ruleID = ruleID;
            return this;
        }


        public Builder startedAt(OffsetDateTime startedAt) {
            Utils.checkNotNull(startedAt, "startedAt");
            this.startedAt = startedAt;
            return this;
        }

        public Evaluation build() {

            return new Evaluation(
                costUnits, createdAt, endedAt,
                error, evidence, id,
                pitPerSource, result, ruleID,
                startedAt);
        }

    }
}
